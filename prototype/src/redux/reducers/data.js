const dataDefaultState = {
  it1: {
    build: null,
    status: null
  },
  it2: {
    build: null,
    status: null
  }
}

export default (state = dataDefaultState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
